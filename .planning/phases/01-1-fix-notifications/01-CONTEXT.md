# Context: Phase 1 — Fix notifications

**Phase:** 01
**Created:** 2026-07-16

## Decisions

- D-01: Исправить `scheduleNotification` так, чтобы уведомление создавалось даже если reminder попал в прошлое (переносить на ближайшее будущее или немедленно).
- D-02: При создании повторяющейся задачи сохранять `notificationId` и синхронизировать его при каждой генерации следующей даты.
- D-03: При редактировании задачи всегда отменять старое уведомление по сохранённому `notificationId` и создавать новое.
- D-04: Заменить `console.log/error` на пользовательно-видимые ошибки или тосты/alert с понятным текстом.

## Deferred Ideas

- Группировка уведомлений на Android — отложено в v2.
- Действия на уведомление — отложено в v2.

## Scope Fence

Фаза ограничена `hooks/useNotifications.ts`, `App.tsx` (часть вызовов уведомлений), `components/Todo/Todo.tsx` (только вызовы уведомлений при завершении повторяющейся задачи) и типами `TodoItem`. Не трогаем рекламу, навигацию по датам и хранилище, кроме чтения/записи `notificationId`.
